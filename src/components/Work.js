import React from 'react'
import {Row, Col, Container} from 'react-bootstrap';
import WorkCard from './WorkCard';

function Work() {
    return (
        <Container>

        </Container>
    )
}

export default Work
